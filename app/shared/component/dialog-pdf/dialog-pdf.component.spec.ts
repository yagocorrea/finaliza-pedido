import { DialogPdfComponent } from './dialog-pdf.component';

describe('DialogPdfComponent', () => {
  let component: DialogPdfComponent;

  beforeEach(() => {
    component = new DialogPdfComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
