import { BrandDetectorDirective } from './brand-detector.directive';

describe('BrandDetectorDirective', () => {
  let directive: BrandDetectorDirective;

  beforeEach(() => {
    directive = new BrandDetectorDirective();
  });

  it('should create', () => {
    expect(directive).toBeTruthy();
  });
});
