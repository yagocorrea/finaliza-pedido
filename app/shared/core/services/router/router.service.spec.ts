import { RouterService } from './router.service';

describe('RouterService', () => {
  const service: RouterService = new RouterService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
