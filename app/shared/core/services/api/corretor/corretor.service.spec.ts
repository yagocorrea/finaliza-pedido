import { CorretorService } from './corretor.service';

describe('CorretorService', () => {
  const service: CorretorService = new CorretorService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
