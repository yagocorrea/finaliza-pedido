import { PageErroComponent } from './page-erro.component';

describe('PageErroComponent', () => {
  let component: PageErroComponent;

  beforeEach(() => {
    component = new PageErroComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
